// arraw function

const apresentarArraw = nome => `Meu nome é ${nome}`;
const soma = (n1, n2) => n1 + n2;

// Array function com + de uma linha

const somaMumerosPequenos = (n1, n2) => {
    if(n1 || n2 > 10) {
        return "Somente números de 1 a 9";
    } else {
        return n1 + n2;
    }
}


